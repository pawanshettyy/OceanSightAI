
# Models and db are defined in app.py to avoid circular imports and table redefinition errors.
# Import them from app.py if needed for utility functions.
# Example:
# from app import db, OceanData, Species, FisheriesData, BiodiversityIndex, Alert, SpeciesObservation
